#include <stdio.h>

/**
 * main - printing the lower case alphabet
 * Return: Always 0
 */
int main(void)
{
	char letter;
	int i;

	for (i = 0; i < 26; i++)
	{
		putchar(letter);
		letter++;
	}
	putchar("\n");
	return (0);
}
